// Your kubectl.
//
// This one file grows for the whole course: every stage adds to the program
// you already have, rather than starting a new one.
//
// Stage 1: print the URL of the API server this program would talk to — found
// the way kubectl finds it, through the standard kubeconfig loading rules.
package main

import "fmt"

func main() {
	fmt.Println("nothing here yet — run `byok8s list` to see the stages")
}
