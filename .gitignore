/bin/
.byok8s-state.json
.aider*

# Every course's reference/work is module byok8s/app, so building it in place
# leaves an `app` binary beside the source a snapshot is taken from.
/courses/*/reference/work/app
